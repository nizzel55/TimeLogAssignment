/**
 * Export combined fixtures that include both page objects and test data.
 * Pages fixture extends testData fixture, so all fixtures are available.
 */
export { test, expect } from './pages.fixture';
